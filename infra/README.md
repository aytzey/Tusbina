# Infra

Bu klasör local geliştirme ve MVP dağıtımı için altyapı başlangıç dosyalarını içerir.

- `nginx/default.conf`: API reverse proxy
- `../docker-compose.yml`: Postgres + Redis + API + Worker + Nginx
